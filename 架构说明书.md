# 微舆（BettaFish）架构说明书

> 基于仓库 `/Users/niuqh/Desktop/github/niuqh/BettaFish` 的代码分析（截至当前提交），总结系统架构、关键流程与运维要点，供开发、部署与二次扩展参考。

## 1. 文档目的
- 统一描述微舆（BettaFish）多智能体舆情分析系统的整体形态与依赖关系。
- 为运维与部署人员提供环境、配置、流程与观测要点。
- 为二开/科研人员提供模块职责、集成边界与扩展方法。

## 2. 系统概述
微舆是一套“采集 → 多智能体协作 → 汇报”闭环的舆情分析平台。用户在主界面输入分析主题后，系统会：
1. 启动三类Agent（Query/Media/Insight）并行分析不同数据域。
2. 通过ForumEngine记录、梳理Agent间讨论。
3. 由ReportEngine整合多方结论与论坛争论，产出带模版的HTML研究报告。
4. 结合MindSpider爬虫与本地数据库，既能实时抓取社媒内容，也能对历史积累的数百万条评论做深挖。

平台核心特征：
- 多Agent协同，带论坛主持人调度机制，避免思路同质化。
- 复合搜索（Tavily/Bocha）+ 私有数据库 + 多模态解析。
- 预置多行业报告模板，支持自定义文案与结构。
- 模块化Python项目，可通过配置切换不同LLM/搜索API。

## 3. 技术栈与运行要求
| 层级 | 技术/依赖 | 说明 |
| --- | --- | --- |
| 接入层 | Flask 2.3 + Flask-SocketIO, Streamlit 1.28 | Flask (`app.py`) 负责统一入口、Socket推送与子进程管理；三个Streamlit App承载Agent可视化。 |
| 协同层 | 多线程 + 日志监听（ForumEngine）、ReportEngine Flask Blueprint | ForumEngine 监听 `logs/*.log`，驱动主持人模型；ReportEngine 以Flask蓝图暴露 `/api/report`。 |
| 智能体 | OpenAI兼容LLM（DeepSeek、Gemini、Kimi、Qwen 等）、Tavily/Bocha搜索 | 每个Agent通过 `llms.LLMClient` 封装API，按配置切换厂商。 |
| 数据层 | PostgreSQL/MySQL、SQLAlchemy/asyncpg/asyncmy、Playwright、MediaCrawler | MindSpider负责采集与入库；InsightEngine通过 `MediaCrawlerDB` 查询。 |
| NLP/ML | Transformers、torch、专用情感模型（`SentimentAnalysisModel/`） | InsightEngine可调用本地多语种情感分析/关键词优化模型。 |
| 观测与日志 | loguru、SocketIO、`logs/` 目录 | 主进程与各Agent分别写日志，ForumEngine与前端实时消费。 |

运行要求（摘自 `README.md` 与 `requirements.txt`）：
- Python 3.9+，建议使用 Conda/uv 管理环境。
- Playwright 浏览器驱动（爬虫模块）。
- 数据库：推荐 PostgreSQL，可切换 MySQL。
- 可选 GPU 以供情感模型或更大LLM使用。
- 外部API：Tavily、Bocha、各LLM厂商（DeepSeek、Moonshot、Gemini、SiliconFlow等）。

## 4. 总体架构
```
┌────────────┐
│ 前端接入层 │ Flask + SocketIO + Jinja2 + 主页面
└────┬───────┘
     │启动指令 / WebSocket
┌────▼───────┐       ┌───────────────────────────────┐
│ 进程管理器 │──────►│ 三个 Streamlit 子应用 (Agent UI) │
│ (app.py)   │◄──────│ 8501/8502/8503                 │
└────┬───────┘       └───────────────────────────────┘
     │日志/状态
┌────▼───────┐
│ ForumEngine│←监听→ logs/{insight,media,query}.log
│ (monitor)  │→主持人LLM→ forum.log
└────┬───────┘
     │论坛总结
┌────▼────────┐
│ ReportEngine │←─ 三个Agent Markdown结果/论坛日志
│ (蓝图+LLM)   │─→ final_reports/*.html
└────┬────────┘
     │数据库查询/素材
┌────▼────────┐
│ 数据采集层  │ MindSpider + MediaCrawlerDB + Sentiment模型
└─────────────┘
```

## 5. 核心模块说明

### 5.1 接入层（`app.py`, `templates/index.html`, `static/`）
- Flask 提供主界面、配置读取/写入（`read_config_values`/`write_config_values`）以及 SocketIO 推送。
- `initialize_system_components()` 会：
  1. 触发 MindSpider 数据库初始化。
  2. 启动/监听三个 Streamlit 应用（`SingleEngineApp/*.py`）并将 stdout 写入各自日志。
  3. 启动 ForumEngine 监控与主持人。
  4. 初始化 ReportEngine 蓝图。
- `SocketIO` 频道：`console_output`（实时日志）、`forum_message`（论坛对话）。

### 5.2 单Agent分析层
各 Agent 结构高度一致：`agent.py` 中注入 `LLMClient`、搜索工具与若干 Node（在 `nodes/` 下定义），再由 Streamlit App 触发 `DeepSearchAgent.research()`。

| Agent | 主要目录/文件 | 职责与依赖 |
| --- | --- | --- |
| QueryEngine | `QueryEngine/agent.py`、`tools/TavilyNewsAgency`、`SingleEngineApp/query_engine_streamlit_app.py` | 使用 Tavily 搜索新闻/图像，适合公开信息面。默认 LLM：DeepSeek；输出 Markdown 到 `query_engine_streamlit_reports/` 并写 `logs/query.log`。 |
| MediaEngine | `MediaEngine/agent.py`、`tools/BochaMultimodalSearch` | 针对多模态信息（短视频、搜索引擎信息卡片），默认 LLM：Gemini 或 MindSpider 配置；依赖 Bocha 搜索。 |
| InsightEngine | `InsightEngine/agent.py`、`tools/MediaCrawlerDB`、`tools/keyword_optimizer.py`、`tools/sentiment_analyzer.py` | 通过 MediaCrawlerDB 读取自建数据库；内置关键词优化（小型Qwen）与情感分析（WeiboMultilingualSentiment）。输出 Markdown 到 `insight_engine_streamlit_reports/`。 |

共同特性：
- Node流水线：`ReportStructureNode → FirstSearchNode → ReflectionNode → SummaryNode → ReportFormattingNode`。
- `state/` 定义段落、引用与反思状态，每段支持多轮“反思搜索”。
- `utils/format_search_results_for_prompt` 负责将搜索结果裁剪后喂给LLM，避免超长上下文。

### 5.3 协同层
- **ForumEngine (`ForumEngine/monitor.py`)**
  - 监听 `logs/insight.log` 等文件，查找 SummaryNode 关键信息。
  - 将 Agent 的总结发言写入 `logs/forum.log`，并每 5 条触发主持人模型（`llm_host.py`）进行调解/提问。
  - `utils/forum_reader.py` 为 ReportEngine/前端提供 HOST 发言读取工具。
- **ReportEngine (`ReportEngine/agent.py`)**
  - 以 `FileCountBaseline` 追踪 `*_streamlit_reports` 目录，确保三个Agent都产出最新 Markdown。
  - `TemplateSelectionNode` 根据主题/行业挑选模板（`report_template/*.md`），`HTMLGenerationNode` 把 Markdown + 论坛摘要 + 模板合成 HTML。
  - 作为 Flask Blueprint (`flask_interface.py`) 暴露 REST API，供主页面一键生成报告。

### 5.4 数据与模型层
- **MindSpider (`MindSpider/main.py`)**
  - 集成 `BroadTopicExtraction`（热点提要）与 `DeepSentimentCrawling/MediaCrawler`（多平台采集）。
  - 使用 Playwright、数据库管理脚本（`schema/init_database.py`）等，支持 PostgreSQL 与 MySQL。
  - 初始化步骤：配置校验 → 数据库连通 → 表结构检查 → 采集任务调度。
- **SentimentAnalysisModel/**
  - 提供多种训练好的情感模型（BERT/Qwen/传统机器学习）。InsightEngine 默认调用 `WeiboMultilingualSentiment`。
- **数据库结构（`MindSpider/schema/mindspider_tables.sql`）**
  - 包含 `daily_news`, `daily_topics` 等表，用于缓存抓取数据与评论全文。

### 5.5 支撑组件
- `config.py`：基于 `pydantic-settings` 的集中配置，支持 `.env` 热加载，涵盖数据库、各Agent LLM、搜索API、论坛主持人模型等。
- `utils/`：重试工具、论坛读取器、GitHub Issue 快捷链接等。
- `tests/`：示例性单元/集成测试入口（可扩展）。

## 6. 关键流程

### 6.1 系统启动
1. 启动 Flask (`python app.py`)。
2. 用户在前端点击“启动系统”，`initialize_system_components()` 依次：初始化数据库、启动三个 Streamlit 子进程、启动 ForumEngine 线程、初始化 ReportEngine。
3. `SocketIO` 推送各子系统启动日志至浏览器。

### 6.2 数据采集（可后台运行）
1. MindSpider 定时或手动执行 `main.py run`。
2. `BroadTopicExtraction` 抓取热点 / 关键词；`DeepSentimentCrawling` 对指定平台深入抓取评论。
3. 数据写入 PostgreSQL/MySQL，供 InsightEngine 查询。

### 6.3 分析任务
1. 用户在主页面输入分析主题 → 通过 query 参数传递到各 Streamlit 子应用。
2. 每个 Agent 自动执行 `DeepSearchAgent.research()`：
   - 生成段落结构。
   - 依次处理段落：初次搜索 → 结果总结 → 反思 → 追加搜索。
   - 写入 Markdown 报告至 `{agent}_streamlit_reports/`，同时输出日志。
3. ForumEngine 监听 `logs/*.log`，抽取 SummaryNode 输出；主持人模型在 `forum.log` 中插话，引导下一轮 Agent 调查。

### 6.4 报告生成
1. 当三个 Agent 均生成最新 Markdown 后，ReportEngine 通过 FileCountBaseline 检测到“齐活”。
2. ReportEngine 读取：
   - Query/Media/Insight 报告 Markdown。
   - `forum.log` 中的主持人总结作为“讨论记录”。
   - 可选自定义模板（`report_template/*.md`）。
3. 由 LLM 将多源信息编织成统一结构、嵌入可视化占位符，输出 HTML 至 `final_reports/final_report__{timestamp}.html`。

## 7. 数据流与存储约定
| 数据 | 生产者 | 消费者 | 存放位置 |
| --- | --- | --- | --- |
| 热点/评论原始数据 | MindSpider 爬虫 | InsightEngine、离线分析 | 外部数据库（PostgreSQL/MySQL） |
| Agent Markdown 报告 | 各 Streamlit App | ReportEngine | `query_engine_streamlit_reports/`, `media_engine_streamlit_reports/`, `insight_engine_streamlit_reports/` |
| 实时日志 | Streamlit 子进程 | ForumEngine、前端控制台 | `logs/{query,media,insight}.log` |
| 论坛对话 | ForumEngine + 主持人 | ReportEngine、前端 | `logs/forum.log` |
| 最终报告 | ReportEngine | 用户/下载 | `final_reports/*.html` |

## 8. 配置与安全
- 所有配置走 `config.py` + `.env`，`app.py` 暴露 `/api/config` 便于前端动态修改。
- 关键字段按 Agent 维度拆分，典型示例：
  - `QUERY_ENGINE_API_KEY/BASE_URL/MODEL_NAME`
  - `MEDIA_ENGINE_*`（Gemini/Bocha）
  - `INSIGHT_ENGINE_*`（Kimi等）
  - `REPORT_ENGINE_*`（用于生成综合报告）
  - `FORUM_HOST_*`（主持人模型）
  - `KEYWORD_OPTIMIZER_*`（关键词优化中间件）
  - `TAVILY_API_KEY`, `BOCHA_WEB_SEARCH_API_KEY`
- `.env` 中需配置数据库凭据；若使用云数据库，要符合日志中的合规要求。
- `write_config_values` 会更新 `.env` 并热加载，注意运行用户需对项目根目录有写权限。

## 9. 部署方案
1. **Docker Compose**（推荐快速体验）  
   - `docker compose up -d` 一键拉起所有服务；可按 README 注释切换镜像源。
   - 容器内包含 Flask 主应用、Streamlit 子应用、数据库。
2. **源码部署**  
   - 创建虚拟环境 → `pip/uv install -r requirements.txt` → `playwright install chromium`。
   - 准备数据库与 `.env` 后，运行 `python app.py`。
   - 采集器 MindSpider、单Agent Streamlit App、ReportEngine 也可独立运行，方便调试。

## 10. 观测与运维
- **日志分层**：`logs/query.log` / `media.log` / `insight.log` / `forum.log` / `report.log`。
- **ForumEngine**：若 forum.log 未刷新，可通过 `stop_forum_engine()` / `start_forum_engine()` 重启监听。
- **进程状态**：`processes` 字典记录每个子进程 PID、端口与状态；前端可查看实时输出。
- **ReportEngine 基准**：`logs/report_baseline.json` 保存各 Agent 最新文件数，便于判定是否需要重新汇总。
- **数据库健康检查**：MindSpider 启动时会执行 ping/table 检查与 `schema/init_database.py`。

## 11. 扩展与二次开发指引
1. **新增 Agent**  
   - 复制任一 Agent 目录，复用 `LLMClient + nodes` 模式。
   - 在 `SingleEngineApp/` 增加对应 Streamlit UI，并在 `app.py` 的 `STREAMLIT_SCRIPTS` 和 `processes` 中注册。
   - 在 ReportEngine 中扩展 `directories` 映射及模板解析逻辑。
2. **替换模型/搜索工具**  
   - 只要兼容 OpenAI API，即可通过 `.env` 中的 `*_BASE_URL/MODEL_NAME` 切换。
   - 关键词优化/情感分析可引入自研模型，替换 `InsightEngine/tools` 内部实现。
3. **扩充报告模板**  
   - 将 Markdown 模板放入 `ReportEngine/report_template/`，命名遵循中文说明，`TemplateSelectionNode` 会通过 LLM 按行业挑选。
4. **接入内部数据**  
   - 在 `MediaCrawlerDB` 中新增数据库连接/查询函数。
   - 也可直接在 InsightEngine 的 `execute_search_tool` 中接入企业内部 API。

## 12. 仓库目录速览
```
app.py                      # Flask主入口
config.py                   # 全局配置（Pydantic Settings）
SingleEngineApp/            # 三个Agent的Streamlit前端
QueryEngine/                # Query Agent 实现及节点
MediaEngine/                # Media Agent 实现
InsightEngine/              # Insight Agent 实现
ReportEngine/               # 报告聚合与模板
ForumEngine/                # 日志监听与主持人
MindSpider/                 # 爬虫 + 数据库初始化
SentimentAnalysisModel/     # 多种情感模型
final_reports/              # 输出HTML报告
logs/                       # 运行日志
*_streamlit_reports/        # 各Agent的Markdown结果
utils/                      # 通用工具
templates/, static/         # Flask前端资源
tests/                      # 测试用例
requirements.txt            # 依赖
```

## 13. 风险与改进建议
- **外部API稳定性**：多家LLM与搜索服务存在限流/延迟，需要在 `tools/` 层强化重试与熔断。
- **日志体量**：多Agent并发可能导致 `logs/*.log` 快速增长，建议加日志轮换或集中化采集。
- **论坛主持人依赖**：ForumEngine 监听解析依赖固定日志格式，若调整 loguru 模板需同步更新正则。
- **数据合规**：云数据库接入需遵循 README 提到的合规审核（2025-10-01 后暂停新增），企业部署时需自建数据源。
- **测试覆盖**：当前 `tests/` 目录较空，建议为 Agent 节点、ReportEngine、ForumEngine 等补充单元/集成测试。

---
如需进一步了解执行细节，可对照以下核心文件：
- `app.py`：系统启动与子进程管理。
- `QueryEngine/agent.py` / `MediaEngine/agent.py` / `InsightEngine/agent.py`：Agent流水线。
- `ReportEngine/agent.py`：报告整合。
- `ForumEngine/monitor.py`：论坛协作机制。
- `MindSpider/main.py`：数据采集。
